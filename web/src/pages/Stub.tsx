export default function NotFound() {
  return (
    <main className="max-w-[1400px] mx-auto px-4 sm:px-6 lg:px-8 py-8">
      <h1 className="text-2xl font-semibold">尚未实现</h1>
      <p className="mt-2 text-gray-500">v0.2 vertical slice 先做选题；这页之后再补。</p>
    </main>
  )
}
