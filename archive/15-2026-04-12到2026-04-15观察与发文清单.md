# 2026-04-12到2026-04-15观察与发文清单

## 这 4 天的目标

- 不再继续给主号叠加新变量
- 把 `2026-04-10` 到 `2026-04-11` 这批书的冷启动看清楚
- 只处理最弱标题，不乱动相对更强的观察样本
- 把下一篇主号新题提前备好，但最早 `2026-04-16` 再决定是否发布

---

## 当前必须盯的 4 本

1. `白月光早产那夜，他们让我给孩子当妈，我先报了警`
2. `他们逼我认下顶流孩子那天，我先晒出了他的结扎证明`
3. `离婚后，前夫的死对头夜夜来接我下班`
4. `直播算命爆火后，全网跪求我别下播`

每天只记这 3 个值：

- `is_data_show`
- `read_count`
- 较前一天是否明显增加

---

## 2026-04-12

### 今天不做什么

- 不发新书
- 不继续开新坑
- 不动 `白月光早产`
- 不动 `顶流孩子`

### 今天要做什么

1. `15:00` 之后看一次后台
2. 记录这 4 本的 `is_data_show` 和 `read_count`
3. 把 `死对头接下班` 列为高优先重写样本
4. 把 `直播算命` 移出主号主推池
5. 只准备下一篇主号新题，不发布

### 今天的判断标准

- 如果 `4` 本全都还是 `is_data_show = 0`，继续停发
- 如果只有个别书阅读动了，但还没转正，继续观察，不加新书

---

## 2026-04-13

### 今天不做什么

- 原则上仍不发新书
- 不因为“昨天没发”就补发一篇

### 今天要做什么

1. `15:00` 之后看第二次后台
2. 对比 `2026-04-12` 的记录
3. 确认 `白月光早产`、`顶流孩子` 是否有转正迹象
4. 把下一篇主号题的标题和前 `30%` 过一遍发前门槛

### 今天的判断标准

- 如果 `白月光早产` 或 `顶流孩子` 有一本变成 `is_data_show = 1`，继续观察，不抢它的流量
- 如果 `死对头接下班` 和 `直播算命` 仍完全不动，维持“重写 / 边缘池”判断

---

## 2026-04-14

### 今天的动作

1. `15:00` 之后看第三次后台
2. 给这 4 本做第一次明确分级：
   - `继续观察`
   - `准备替换标题`
   - `边缘池暂缓`
3. 检查下一篇主号题是否满足发前门槛

### 发前门槛

- 标题压强 `10` 分制至少 `8` 分
- 字数尽量在 `9000-12000`
- 前 `30%` 内必须出现：
  - 受辱场
  - 反击动作
  - 更大威胁

### 今天的判断标准

- 如果到今天为止仍然全部 `is_data_show = 0` 且阅读在 `0-个位数`，下一批继续停发到 `2026-04-15` 再定
- 如果已有一本明显动起来，也先不急着发布新题，优先等这批跑出结论

---

## 2026-04-15

### 今天的动作

1. `15:00` 之后做第四次后台记录
2. 对这 4 本做最终处理决定
3. 决定 `2026-04-16` 能不能发新书

### 最终决策规则

#### 继续观察

满足任一条即可：

- `is_data_show = 1`
- 阅读开始明显增长
- 已脱离 `0-个位数`

#### 视为这轮初推没过

同时满足以下两条：

- 仍然 `is_data_show = 0`
- 阅读仍是 `0-个位数`

#### 2026-04-16 是否允许发新书

- 允许：只有在这批里至少有 `1` 本出现明确转正迹象时，才允许发 `1` 本主号新题
- 不允许：如果这批整体还没跑明白，就继续停发，不新增变量

---

## 这 4 天唯一允许准备的新题方向

- 婚姻家庭
- 追妻火葬场
- 白月光
- 私生子
- 亲子鉴定
- 豪门婚礼
- 坐牢翻案
- 当场公开翻盘

暂时不作为主号首发：

- AI 职场脑洞
- 直播泛爽文
- 规则怪谈
- 泛悬疑惊悚

---

## 每日记录模板

### 日期

- `is_data_show`
- `read_count`
- 变化判断：

### 四本记录

- `白月光早产`：
- `顶流孩子`：
- `死对头接下班`：
- `直播算命`：

### 当天动作

- 是否发新书：
- 是否继续观察：
- 是否进入重写：
- 备注：
